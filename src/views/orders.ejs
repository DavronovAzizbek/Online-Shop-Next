<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Buyurtmalar</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style/orders.css">
  </head>
  <body>
    <div class="container">
      <h1 class="text-center mb-5">Buyurtma berish</h1>

      <div class="order d-flex align-items-center justify-content-between">
        <ul>
          <li>Nomi: <%= product.name %></li>
          <li>Narxi: <%= product.price %></li>
          <li>Tavsif: <%= product.desc %></li>
          <li><img width="100" height="100" src=<%= product.img %> alt=<%= product.name %>></li>
        </ul>
         <form class="w-100" action="/orders/create" method="post">
            <select class="form-control mb-3" name="product_id">
                <option selected value=<%= product.id %>><%= product.name %></option>
            </select>
        <input required name="count" class="form-control mb-3" type="number" min="1" max="10" placeholder="Buyurtma soni">
        <input required name="phone" class="form-control mb-3" type="tel" placeholder="Telefon raqam">
        <input required name="address" class="form-control mb-3" type="text" placeholder="Manzil kiriting">
        <button class="btn btn-success">Buyurtma berish</button>
      </form>
      </div>

     
    </div>
  </body>
</html>
